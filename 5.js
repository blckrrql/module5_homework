let myArr = [23,25,27,19,22,8,28,22,1];
console.log('Всего элементов в массиве: ' + myArr.length);
console.log('Элементы массива:');
for (let i = 0; i < myArr.length; i ++) {
  console.log(myArr[i]);
}